[project]
name = "skew"
version = "0.1.0"
requires-python = ">=3.11"
description = "Catch training-serving skew between Postgres feature history and Redis."

[tool.pytest.ini_options]
pythonpath = ["src"]
